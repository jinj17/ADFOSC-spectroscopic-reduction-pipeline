Place a small sample dataset here (or point `workdir` in your config
at wherever your real data lives) if you want `examples/fileinfo_example.yaml`
to work out of the box for new users trying the pipeline.
